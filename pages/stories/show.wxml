<view class="section hero small">

  <view class="content centered">
    <view class="header">
      {{ entity.header }}
      <!-- <view class="sub-header">{{ entity.original_header }}</view> -->
    </view>
    <view wx:for="[1,2,3]" wx:key="key">
      <image data-vid="{{ entity.id }}" src="{{ entity.image }}" mode="aspectFill">
        <view class="label">{{ entity.duration }}</view>
      </image>
  </view>
    <!-- <view class="description">{{ entity.description }}</view> -->
  </view>
</view>
